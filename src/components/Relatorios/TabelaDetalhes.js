import React from 'react';
export default function TabelaDetalhes({ linhas }) {
  return <div style={{marginTop: '30px', padding: '20px', background: '#fff', borderRadius: '12px', border: '1px solid #e2e8f0'}}>Tabela de Detalhes (Mock)</div>;
}
